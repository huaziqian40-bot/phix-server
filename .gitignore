# ============================================================================
# phix 服务端 —— 绝不入库的东西
#
# 原则：**只上传本地的源码**。生产机上的真实用户数据一律不进仓库，
# 与心履（xinlv-web 等）的做法一致。
#
# 注意：.gitignore 里 `#` 只有**在行首**才是注释。行尾写 `pattern  # 说明`
# 会把说明当成模式的一部分，那条规则等于没写（这个坑踩过一次）。
# ============================================================================

# ---- 真实用户数据 ----

# 账号、Bearer 令牌、refresh 令牌哈希，以及端到端加密后的密文对象都在这里。
# 服务端解不开密文，但账号与令牌是实打实的用户数据 —— 绝不入库。
db.sqlite3

# ---- 密钥 ----

# 部署时生成的令牌签名密钥（改了 → 已发出的令牌全部失效）
secret_key.txt

# 与官网 / 心履之间的机器间密钥
.service_key

# 部署凭据（PHIX_DEPLOY_HOST / USER / PASSWORD）
.deploy_secret

# 本机联调用密钥
e2e_key.txt
jwt_key.txt

# ---- 日志与依赖 ----
logs/
*.log
.venv/
venv/
__pycache__/
*.pyc
.env
